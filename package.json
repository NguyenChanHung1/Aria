{
  "name": "aria",
  "version": "0.1.0",
  "private": true,
  "description": "AI agent monorepo for helping non-experts create songs",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:web": "pnpm --filter @aria/web dev",
    "dev:services": "docker compose up agent lyrics composition mixing postgres redis -d"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
